[env:esp32]
platform = espressif32
board = esp32doit-devkit-v1
framework = arduino
monitor_speed = 115200

lib_deps =
    bogde/HX711@^0.7.4

# For other ESP32 boards, use:
# board = esp32-s3-devkitm-1  (for ESP32-S3)
# board = esp-wrover-kit       (for older boards)
